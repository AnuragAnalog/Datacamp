--Instruction 1
SELECT imdb_score, film_id
FROM reviews
ORDER BY imdb_score DESC

--Instruction 2
SELECT title
FROM films
ORDER BY title DESC

--Instruction 3
SELECT title, duration
FROM films
ORDER BY duration DESC

